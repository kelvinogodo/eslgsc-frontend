import PasswordSetupForm from '../../components/portal/PasswordSetupForm';

/** The "sign up" step: invited staff land here from their email to activate their account. */
const SetPassword = () => <PasswordSetupForm mode="invite" />;

export default SetPassword;
