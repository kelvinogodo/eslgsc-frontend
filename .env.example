## Example environment variables for local development
## Copy this file to `.env` and update values as needed. Do NOT commit your real `.env` to the repository.

# Base URL for the API (include /api if your backend uses that path)
VITE_API_BASE_URL=http://localhost:5000/api

# Example: Enable debug features in dev
# VITE_ENABLE_DEBUG=true

# Add other VITE_* variables here as required by the app
